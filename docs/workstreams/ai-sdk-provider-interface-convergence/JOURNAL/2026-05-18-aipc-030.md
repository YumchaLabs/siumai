# 2026-05-18 - AIPC-030 Core And Registry Guard Slice

Completed AIPC-030.

Changes:

- Added a `siumai-core::standards` integration boundary guard in
  `siumai-core/tests/core_provider_boundary_test.rs`.
- The guard rejects provider/protocol island subdirectories and provider-named modules under
  `siumai-core/src/standards`.
- Reconfirmed the existing `siumai-registry` stable-family handle guard.

Important finding:

- A stale empty local directory existed at `siumai-core/src/standards/openai`.
- It was not tracked by Git and contained no files.
- The new guard correctly rejected it, so it was removed from the working tree.

Validation:

- `cargo fmt -p siumai-core --check`
- `cargo nextest run -p siumai-core core_standards_module_does_not_reintroduce_provider_protocol_islands --no-fail-fast`
- `cargo nextest run -p siumai-registry stable_registry_handles_do_not_use_compat_client_paths_for_primary_family_execution --no-fail-fast`
- `cargo nextest run -p siumai-registry --no-fail-fast`
- `cargo nextest run -p siumai-core --no-fail-fast`

Next task:

- AIPC-040: audit whether any registry stable-family primary paths still depend on compatibility
  clients where native family models already exist.

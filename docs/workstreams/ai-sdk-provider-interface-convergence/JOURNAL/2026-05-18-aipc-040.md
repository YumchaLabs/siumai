# 2026-05-18 - AIPC-040 Registry Compatibility Handle Boundary

Completed AIPC-040.

Changes:

- Added `remaining_registry_handle_compat_paths_are_extension_only` in
  `siumai-registry/src/registry/entry/boundary_tests.rs`.
- Kept the existing primary stable-family guard and added explicit checks for the remaining image
  and audio compatibility-client paths.
- Locked image compatibility access to `ImageExtras` edit/variation helpers.
- Locked speech/transcription compatibility access to extension helpers for streaming and extra
  audio operations, while keeping primary text-to-speech and speech-to-text methods native.

Important finding:

- No forced registry code move was needed. The remaining compatibility paths are not primary stable
  family execution paths; they are extension-only behaviors that do not yet have a stable family
  trait seam.

Validation:

- `cargo fmt -p siumai-registry --check`
- `cargo nextest run -p siumai-registry remaining_registry_handle_compat_paths_are_extension_only --no-fail-fast`
- `cargo nextest run -p siumai-registry --no-fail-fast`

Next task:

- AIPC-050: audit stream event semantics and migrate one provider slice toward stable
  `ChatStreamEvent::Part(ChatStreamPart)` assertions where AI SDK stable semantics already exist.

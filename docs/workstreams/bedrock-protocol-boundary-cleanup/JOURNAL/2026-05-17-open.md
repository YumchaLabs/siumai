# 2026-05-17 - Open Workstream

Opened Bedrock protocol boundary cleanup as a follow-on after closing fearless-refactor-v4.

Initial audit: `standards/bedrock/chat.rs` is the largest provider-local protocol file and mixes
production request/response/stream code with an extensive inline contract suite. First slice is test
ownership isolation because it is behavior-preserving and directly improves locality before deciding
whether production request/response/stream extraction is justified.

BPC-020 completed in the opening session. The inline test suite now lives in `chat/tests.rs`, the
production shell keeps only `mod tests;`, and the focused Bedrock no-network gate passed with 74
tests.

BPC-030 completed after a production boundary audit. Stream conversion moved to `chat/streaming.rs`
because its DTOs, accumulator state, raw chunk handling, terminal event generation, and
`JsonEventConverter` implementation form a coherent protocol boundary. Request planning and
non-stream response shaping stayed in `chat.rs`; their remaining helpers are shared Bedrock protocol
mapping code, so splitting them now would mostly add private cross-module glue. Focused Bedrock
gate passed again with 74 tests.

BPC-040 closed the lane. No immediate follow-on was split. Future Bedrock cleanup should be driven
by a concrete behavior or reuse issue, not by the remaining size of `chat.rs`.

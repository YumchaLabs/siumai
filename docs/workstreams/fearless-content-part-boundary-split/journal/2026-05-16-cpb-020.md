# CPB-020 Journal

Date: 2026-05-16

## Summary

Refreshed the direct `ContentPart` / provider-map construction scan across core, bridge, protocol,
provider, and facade production sources.

The scan found:

- 121 paths already classified by the previous spec/core convergence audit.
- 48 low-priority or false-positive paths that had previously been hidden by broad guard buckets.
- 0 unclassified high-value production paths.

## Change

Added `direct-content-part-scan.md` to make those 48 paths explicit, then tightened
`facade_architecture_boundary_test::content_part_provider_map_audit_covers_high_value_production_hits`
so future hits must appear in either the previous audit or the new scan document. Broad suffix and
directory auto-allow rules were removed.

## Validation

- `cargo fmt --package siumai --check`
- `cargo nextest run -p siumai --test facade_architecture_boundary_test content_part_provider_map_audit_covers_high_value_production_hits --features openai,anthropic,google --no-default-features --no-fail-fast`
- `cargo nextest run -p siumai-spec --test content_projection_boundary_test --no-default-features --no-fail-fast`
